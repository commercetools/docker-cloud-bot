{
  "name": "docker-cloud-bot",
  "version": "1.0.0",
  "description": "A bot to manage your Docker cloud stacks (create, update, delete) based on PR lifecycle",
  "maintainers": [
    "MC Support <mc@commercetools.com>"
  ],
  "license": "ISC",
  "repository": "https://github.com/commercetools/docker-cloud-bot.git",
  "scripts": {
    "start": "probot run ./index.js",
    "test": "mocha && xo --extend probot"
  },
  "dependencies": {
    "node-fetch": "^1.7.2",
    "probot": "^12.3.1"
  },
  "devDependencies": {
    "eslint-config-probot": ">=0.1.0",
    "expect": "^1.20.2",
    "localtunnel": "^1.8.2",
    "mocha": "^3.2.0",
    "xo": "^0.54.2"
  },
  "engines": {
    "node": ">= 8.0.0",
    "npm": ">= 5.0.0"
  }
}
